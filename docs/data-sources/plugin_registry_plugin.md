# cycloid_plugin_registry_plugin (Data Source)

Look up an existing plugin catalog entry by name within a registry. Use this data source to
reference a plugin that is managed in another Terraform workspace or created outside Terraform.


## Example Usage

```terraform
// Look up an existing plugin catalog entry by name within a known registry.
// Use when the plugin entry is managed elsewhere and you only need to publish
// a new version or reference the plugin ID.
data "cycloid_plugin_registry" "shared" {
  organization = "acme-platform"
  name         = "internal-registry"
}

data "cycloid_plugin_registry_plugin" "hello" {
  organization = "acme-platform"
  registry_id  = data.cycloid_plugin_registry.shared.id
  name         = "hello-world"
}

resource "cycloid_plugin_version" "hello_v2" {
  organization = "acme-platform"
  registry_id  = data.cycloid_plugin_registry_plugin.hello.registry_id
  plugin_id    = data.cycloid_plugin_registry_plugin.hello.id
  url          = "registry.example.com/hello-world:2.0.0"
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) The display name of the plugin to look up.
- `registry_id` (Number) The ID of the plugin registry to search within.

### Optional

- `organization` (String) The organization canonical, defaults to the provider `default_organization`.

### Read-Only

- `id` (Number) The numeric ID of the plugin.
- `owned` (Boolean) Whether this plugin is owned by the organization.
