---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "cycloid_inventory_values Data Source - cycloid"
subcategory: ""
description: |-
  This datasource allows you to fetch inventory values from the Cycloid inventory.
---

# cycloid_inventory_values (Data Source)

This datasource allows you to fetch inventory values from the Cycloid inventory.

## Example Usage

```terraform
# Filter inventory values from a specific organization
data "cycloid_inventory_values" "shared_vpcs" {
  organization = "parent-organization"
  filters = [
    {
      attribute = "provider"
      condition = "eq"
      value     = "aws"
    },
    {
      attribute = "type"
      condition = "eq"
      value     = "aws_vpc"
    },
    {
      attribute = "project_canonical"
      condition = "eq"
      value     = "shared-network"
    }
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) List of LHS filters to apply to the inventory values. See the docs [here](https://docs.cycloid.io/reference/api/LHS-filters) (see [below for nested schema](#nestedatt--filters))
- `organization` (String) The canonical of the organization where to fetch inventory values, default to the provider's organization.

### Read-Only

- `values` (Dynamic) A list of inventory values matching the filters.

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `attribute` (String) The name of the attribute to filter.
- `condition` (String) The condition to apply, one of "eq", "neq", "gt", "lt", "rlike" or "in".
- `value` (String) The value of the filter.
