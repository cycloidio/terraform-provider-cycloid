---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "cycloid_inventory_value Data Source - cycloid"
subcategory: ""
description: |-
  This datasource allows you to fetch a single inventory value from the Cycloid inventory.
---

# cycloid_inventory_value (Data Source)

This datasource allows you to fetch a single inventory value from the Cycloid inventory.

## Example Usage

```terraform
# Filter a single inventory value from a specific organization
data "cycloid_inventory_value" "shared_vpc" {
  organization = "parent-organization"
  select_first = true
  filters = [
    {
      attribute = "provider"
      condition = "eq"
      value     = "aws"
    },
    {
      attribute = "type"
      condition = "eq"
      value     = "aws_vpc"
    },
    {
      attribute = "project_canonical"
      condition = "eq"
      value     = "shared-network"
    },
    {
      attribute = "environment_canonical"
      condition = "eq"
      value     = "prod"
    }
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Attributes List) List of LHS filters to apply to the inventory value. See the docs [here](https://docs.cycloid.io/reference/api/LHS-filters) (see [below for nested schema](#nestedatt--filters))
- `organization` (String) The canonical of the organization where to fetch inventory value, default to the provider's organization.
- `select_first` (Boolean) If more than one inventory value is listed, the data will fail, set this attribute to `true` to select the first result.

### Read-Only

- `value` (Dynamic) A single inventory value matching the filters.

<a id="nestedatt--filters"></a>
### Nested Schema for `filters`

Required:

- `attribute` (String) The name of the attribute to filter.
- `condition` (String) The condition to apply, one of "eq", "neq", "gt", "lt", "rlike" or "in".
- `value` (String) The value of the filter.
